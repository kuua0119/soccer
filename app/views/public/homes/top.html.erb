<div class="hero d-flex align-items-center justify-content-center text-white">
  <div class="overlay p-4 p-md-5 rounded text-center">
    <h1 class="display-3 fw-bold mb-2">サカコミ ⚽</h1>
    <p class="lead mb-4">世界中のサッカーファンが集まるコミュニティサイト</p>
  </div>
</div>

    <% if user_signed_in? %>
      <!-- ログイン後 -->
      <div class="mx-auto text-center">
        <h5 class="text-dark bg-light py-4 mb-0">ようこそ！サッカーファン同士でつながり、試合をもっと楽しもう！</h5>
      </div>
    <% else %>
      <!-- ログイン前 -->
      <div class="d-flex justify-content-center gap-2">
        <%= link_to "新規登録", new_user_registration_path, class: "btn btn-primary btn-lg shadow" %>
        <%= link_to "ログイン", new_user_session_path, class: "btn btn-outline-light btn-lg shadow" %>
      </div>
    <% end %>
  </div>
</div>

<div class="container my-5">
  <div class="bg-primary text-white p-2 text-center">最新の投稿</div>

  <% if @posts.any? %>
    <div class="row">
      <% @posts.each do |post| %>
        <div class="col-md-6 mb-4">
          <div class="card shadow-sm">
            <div class="card-body">
              <h5 class="card-title"><%= link_to post.title, post_path(post) %></h5>
              <p class="card-text text-muted">
                <%= post.user&.name || "退会済みユーザー" %> さん | 
                <%= l post.created_at, format: :short %>
              </p>
              <p><%= truncate(post.body, length: 100) %></p>
              <% if post.image.attached? %>
                <%= image_tag post.image, class: "img-fluid rounded" %>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-muted text-center">まだ投稿はありません。</p>
  <% end %>
</div>

<footer class="bg-dark text-white text-center py-3 mt-5">
  <p class="mb-0">© <%= Time.current.year %> サカコミ | 
    <%= link_to "About", about_path, class: "text-white text-decoration-underline" %>
  </p>
</footer>